# GENERATED by src/helix_arm/generate_scenes.py. Do not edit: a test asserts this
# file is byte-exactly what the generator emits, and the hardened/legacy relation
# holds by construction rather than by diff discipline.
directives:

# The arm, welded at the world ORIGIN -- see the module docstring.
  - add_model:
      name: helix7_p025
      file: package://combining_kinematics/models/helix7_p025/helix7_p025.sdf

  - add_weld:
      parent: world
      child: helix7_p025::base_link

  - add_model:
      name: finray
      file: package://combining_kinematics/models/wsg_finray/wsg50_110_finray_fingers_box_collision.sdf

  - add_weld:
      parent: helix7_p025::flange
      child: finray::hand
      X_PC:
        translation: [0, 0, 0.1]
        rotation: !Rpy { deg: [90.0, 0.0, 90.0] }

  - add_model:
      name: binF
      file: package://drake_models/manipulation_station/bin2.sdf

  - add_weld:
      parent: world
      child: binF::bin_base
      X_PC:
        translation: [0.75, 0, 0]
        rotation: !Rpy { deg: [0.0, 0.0, 0.0] }

  - add_model:
      name: table
      file: package://drake_models/manipulation_station/table_wide.sdf

  - add_weld:
      parent: world
      child: table::table_body
      X_PC:
        translation: [0.4, 0.0, 0.0]

  - add_model:
      name: table2
      file: package://drake_models/manipulation_station/table_wide.sdf

  - add_weld:
      parent: world
      child: table2::table_body
      X_PC:
        translation: [-0.2, 0.0, 0.0]

  - add_model:
      name: shelves
      file: package://combining_kinematics/models/assets/shelves.sdf

  - add_weld:
      parent: world
      child: shelves::shelves_body
      X_PC:
        translation: [0.3, 0.55, 0.4]
        rotation: !Rpy { deg: [0.0, 0.0, 235.0] }

  - add_model:
      name: shelves2
      file: package://combining_kinematics/models/assets/shelves.sdf

  - add_weld:
      parent: world
      child: shelves2::shelves_body
      X_PC:
        translation: [0.3, -0.55, 0.4]
        rotation: !Rpy { deg: [0.0, 0.0, 135.0] }

  - add_model:
      name: shelves3
      file: package://combining_kinematics/models/assets/shelves.sdf

  - add_weld:
      parent: world
      child: shelves3::shelves_body
      X_PC:
        translation: [-0.45, -0.55, 0.4]
        rotation: !Rpy { deg: [0.0, 0.0, 235.0] }

  - add_model:
      name: shelves4
      file: package://combining_kinematics/models/assets/shelves.sdf

  - add_weld:
      parent: world
      child: shelves4::shelves_body
      X_PC:
        translation: [-0.45, 0.55, 0.4]
        rotation: !Rpy { deg: [0.0, 0.0, 135.0] }

